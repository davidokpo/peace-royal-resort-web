{

  "name": "monorepo",

  "version": "0.0.0",

  "private": true,

  "scripts": {
    "dev": "concurrently --kill-others --raw \"npm run dev --workspace apps/web\" \"npm run dev --workspace api\"",
    "build": "npm run build --workspace apps/web",
    "vercel-build": "npm run build --workspace apps/web",
    "start": "node api/src/main.js",
    "lint": "npm run lint --workspace apps/web && npm run lint --workspace api"
  },

  "devDependencies": {

    "concurrently": "^9.1.2"

  },

  "workspaces": [

    "apps/*",
    "api"

  ]

}
